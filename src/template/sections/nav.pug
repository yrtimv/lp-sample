nav.nav
  - var items = ['Home', 'About us', 'Our approach', 'Our work']
  each item, idx in items
    if idx === 0
      a.nav__link.nav__link--active(href='#')= item
    else
      a.nav__link(href='#')= item
